"""
为每条新闻提取精简关键词（15-20个）
从 content_cut 中过滤无意义词汇，保留最有代表性的关键词
"""

import pandas as pd
import jieba
from collections import Counter
import re


# ==================== 使用与 extract_hot_topics.py 相同的停用词表 ====================

# 1. 地域名称
LOCATION_WORDS = {
    '广州', '广东', '广州市', '广东省', '粤', '羊城', '花城',
    '深圳', '深圳市', '珠海', '珠海市', '佛山', '佛山市',
    '东莞', '东莞市', '中山', '中山市', '惠州', '惠州市',
    '江门', '江门市', '肇庆', '肇庆市', '清远', '清远市',
    '汕头', '汕头市', '湛江', '湛江市', '茂名', '茂名市',
    '韶关', '韶关市', '河源', '河源市', '梅州', '梅州市',
    '汕尾', '汕尾市', '阳江', '阳江市', '云浮', '云浮市',
    '潮州', '潮州市', '揭阳', '揭阳市',
    '中国', '北京', '上海', '天津', '重庆',
    '香港', '澳门', '台湾',
}

# 2. 媒体/报社相关词汇
MEDIA_WORDS = {
    '记者', '本报', '编辑', '通讯员', '广州日报', '日报',
    '南方日报', '信息时报', '新快报', '羊城晚报', '南方都市报',
    '澎湃新闻', '新华社', '人民日报', '央视新闻', '凤凰卫视',
    '新浪网', '腾讯网', '网易', '搜狐', '今日头条',
    '公众号', '微信', '微博', '抖音', '快手',
    '报道', '采访', '新闻', '消息', '快讯', '直播',
    '主播', '主持人', '评论员', '特约', '专栏',
}

# 3. 领导人姓名
LEADER_NAMES = {
    '习近平', '李克强', '李强', '汪洋', '王沪宁',
    '赵乐际', '韩正', '丁薛祥', '陈希', '胡春华',
    '刘鹤', '孙春兰', '杨洁篪', '郭声琨', '黄坤明',
    '蔡奇', '王毅', '肖捷', '郑建邦', '何立峰',
    '王小洪', '吴政隆', '谌贻琴', '总书记', '总理', '主席'
}

# 4. 通用的无意义动词/形容词/抽象词
GENERIC_WORDS = {
    # 泛化动词
    '建设', '推动', '发展', '提高', '加强', '促进', '开展',
    '进行', '实现', '坚持', '努力', '推进', '落实', '做好',
    '完成', '实施', '执行', '组织', '安排', '部署',
    '要求', '强调', '指出', '提出', '表示', '认为', '说明',
    '介绍', '了解', '据悉', '通报', '发布', '公布',
    
    # 会议/活动通用词
    '举行', '召开', '开幕', '闭幕', '启动', '举办',
    '参加', '出席', '莅临', '座谈', '调研', '考察',
    '会见', '接见', '慰问', '祝贺', '庆祝',
    
    # 公文常用词
    '关于', '贯彻', '学习', '精神', '公告', '通知',
    '决定', '意见', '方案', '规划', '计划', '总结',
    '报告', '讲话', '发言', '致辞', '指示',
    
    # 泛化形容词/副词
    '积极', '重要', '主要', '良好', '广大', '全面', '深入',
    '持续', '进一步', '不断', '加快', '大力', '认真',
    '切实', '有效', '显著', '突出', '明显', '稳步',
    
    # 泛指名词/代词
    '群众', '市民', '人们', '我们', '他们', '大家', '各位',
    '这个', '那个', '这些', '那些', '其中', '上述', '以下',
    '有关', '相关', '目前', '当前', '今后', '未来',
    
    # 其他高频但无信息量的词
    '工作', '产业', '城市', '服务', '企业', '单位', '部门',
    '项目', '活动', '会议', '情况', '问题', '方面', '领域',
    '水平', '能力', '质量', '效益', '成果', '经验', '做法',
    
    # 不完整的词
    '全运', '大湾', '十五',
}

# 5. 时间词
TIME_WORDS = {
    '今日', '昨日', '明天', '今天', '昨天', '日前', '此前',
    '现在', '未来', '过去', '年度', '月度', '上午', '下午',
    '时分', '时刻', '时候', '期间', '时期', '阶段',
    '今年', '去年', '明年', '当月', '本月', '上月',
    '近期', '远期', '长期', '短期', '暂时', '随时',
}

# 合并所有停用词
ALL_STOPWORDS = (
    LOCATION_WORDS | MEDIA_WORDS | LEADER_NAMES | 
    GENERIC_WORDS | TIME_WORDS
)


def load_custom_words():
    """加载自定义词典，确保专有名词完整"""
    custom_words = [
        # 体育赛事
        '全运会', '十五运会', '十五届全运会', '残运会', '特奥会',
        '亚运会', '奥运会', '世界杯', '锦标赛',
        # 区域发展
        '粤港澳大湾区', '大湾区', '前海', '横琴', '南沙',
        '珠三角', '长三角', '京津冀', '成渝',
        # 会议展会
        '广交会', '两会', '四次会议', '十四届', '人大会议', '政协会议',
        '中央经济工作会议', '省委全会', '市委全会',
        '党代会', '人代会', '政协会',
        # 政策概念
        '高质量', '高质量发展', '现代化', '中国式现代化',
        '新发展格局', '新发展理念', '双循环',
        '共同富裕', '乡村振兴', '生态文明',
        # 科技产业
        '人工智能', 'AI', '数字经济', '科技创新', '智能制造',
        '集成电路', '生物医药', '新能源汽车', '低空经济',
        '大数据', '云计算', '物联网', '区块链', '元宇宙',
        '5G', '6G', '芯片', '半导体',
        # 节庆活动
        '新春', '春节', '元宵', '中秋', '国庆',
        '花市', '庙会', '灯会', '春晚',
        # 经济金融
        '股市', '楼市', '房市', '债市', '汇市',
        'GDP', 'CPI', 'PPI', 'PMI',
        # 其他专有名词
        '广货', '粤菜', '岭南', '珠澳', '深港',
        '一带一路', '自贸区', '保税区', '开发区',
    ]
    for word in custom_words:
        jieba.add_word(word)


def extract_keywords(text, top_n=18):
    """
    从文本中提取Top N个最有意义的关键词
    
    参数:
        text: 输入文本（可以是原文或已分词的文本）
        top_n: 返回的关键词数量（默认18个，范围15-20）
    
    返回:
        空格分隔的关键词字符串
    """
    if not isinstance(text, str) or not text.strip():
        return ''
    
    # 对文本进行分词
    words = jieba.cut(text)
    
    # 统计词频并过滤
    word_freq = Counter()
    for word in words:
        word = word.strip()
        
        # 过滤规则
        if len(word) < 2:  # 单字词
            continue
        if word in ALL_STOPWORDS:  # 停用词
            continue
        if word.isdigit():  # 纯数字
            continue
        if re.match(r'^[\W_]+$', word):  # 纯标点
            continue
        
        word_freq[word] += 1
    
    # 取Top N
    top_words = word_freq.most_common(top_n)
    
    # 用空格连接
    return ' '.join([word for word, freq in top_words])


def process_csv(input_file='抓取并清洗完成的6703条新闻.csv',
               output_file='news_with_keywords.csv'):
    """
    处理CSV文件，为每条新闻添加keywords列
    
    参数:
        input_file: 输入文件路径
        output_file: 输出文件路径
    """
    print("=" * 70)
    print("为每条新闻提取精简关键词（15-20个）")
    print("=" * 70)
    
    # 加载自定义词典
    print("\n【步骤1】加载自定义词典...")
    load_custom_words()
    print(f"  ✓ 已加载 {len(ALL_STOPWORDS)} 个停用词")
    
    # 读取数据
    print(f"\n【步骤2】读取数据: {input_file}")
    try:
        df = pd.read_csv(input_file, encoding='utf-8-sig')
        print(f"  ✓ 成功读取 {len(df)} 条记录")
    except Exception as e:
        print(f"  ❌ 错误: 读取文件失败 - {e}")
        return
    
    # 检查必需的列
    if 'content' not in df.columns:
        print("  ❌ 错误: 数据中缺少 'content' 列")
        return
    
    # 提取关键词
    print(f"\n【步骤3】为每条新闻提取关键词...")
    print(f"  正在处理，请稍候...\n")
    
    keywords_list = []
    for idx, row in df.iterrows():
        content = row['content']
        keywords = extract_keywords(content, top_n=18)
        keywords_list.append(keywords)
        
        # 每1000条显示进度
        if (idx + 1) % 1000 == 0:
            print(f"  已处理 {idx + 1}/{len(df)} 条")
    
    # 添加keywords列
    df['keywords'] = keywords_list
    
    # 保存结果
    print(f"\n【步骤4】保存结果到 {output_file}")
    df.to_csv(output_file, index=False, encoding='utf-8-sig')
    print(f"  ✓ 已保存 {len(df)} 条记录")
    
    # 显示示例
    print(f"\n{'='*70}")
    print("处理完成！示例展示：")
    print(f"{'='*70}\n")
    
    for i in range(min(3, len(df))):
        print(f"第 {i+1} 条新闻：")
        print(f"  标题: {df.iloc[i]['title'][:50]}...")
        print(f"  关键词: {df.iloc[i]['keywords']}")
        print()
    
    print(f"{'='*70}")
    print(f"  输入文件: {input_file}")
    print(f"  输出文件: {output_file}")
    print(f"  总记录数: {len(df)}")
    print(f"{'='*70}\n")


if __name__ == '__main__':
    process_csv()
